<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Chequeo de Puerto</title>
  <style>
    body { font-family: sans-serif; margin: 2rem; }
    .ok { color: green; }
    .fail { color: red; }
    .raw-html {
      background: #f5f5f5;
      padding: 1rem;
      border: 1px solid #ddd;
      max-height: 400px;
      overflow: auto;
      white-space: pre-wrap;
      word-wrap: break-word;
    }
    a { text-decoration: none; color: #007bff; }
  </style>
</head>
<body>
  <h1>Puerto <%= PortController::PORT %> en <%= PortController::HOST %></h1>
  <% if @open %>
    <p class="ok">✅ ¡Puerto abierto!</p>
  <% else %>
    <p class="fail">❌ No se pudo conectar.</p>
  <% end %>

  <h2>HTML recuperado</h2>
  <div class="raw-html"><%= raw @html %></div>

  <p><a href="<%= root_path %>">↻ Reintentar</a></p>
</body>
</html>
